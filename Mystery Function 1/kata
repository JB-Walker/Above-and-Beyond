function solved(str){
  let newStr = str;
  if (str.length > 1) {
    if (str.length % 2 === 1) {
      newStr = str.substring(0, Math.floor(str.length / 2) - 1) + 
        str.substring(Math.ceil(str.length / 2));
      }
    return newStr.split('').sort().join('').trim();
  }
 return '';
}